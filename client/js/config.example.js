// Optional override. In production the recommended setup uses the same-origin /api path
// and client/vercel.json proxies /api/* to the Render backend.
// Only use a full URL here when intentionally bypassing the Vercel proxy.
window.DEVNOTES_API_BASE = "/api";
